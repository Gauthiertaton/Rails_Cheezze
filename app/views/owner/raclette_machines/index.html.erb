<div class="d-flex">
  <%= link_to "Retourner à la liste des appareils", raclette_machines_path, class: "btn btn-outline-warning font-weight-bold m-4" %>
  <%= link_to "Créer une annonce", new_raclette_machine_path, class: "btn btn-outline-warning font-weight-bold m-4" %>
</div>
<h1 class = "text-center mt-2"><strong> Mes appareils </strong></h1>

<div class="row d-flex justify-content-center mr-0">
  <% @raclette_machines.each do |raclette_machine| %>
    <div class="card-resa d-flex">
      <%= cl_image_tag raclette_machine.photo.key, height: 200, width: 300, crop: :fill %>
      <div class="card-resa-infos p-4">
        <p> 🧀 <strong><%= raclette_machine.name %></strong> </p>
        <p class="text-justify"><strong><%= raclette_machine.price %></strong> </p>
        <p class="text-justify"><strong><%= raclette_machine.capacity %></strong> x <i class="fas fa-male"></i></p>

        <br>
      </div>
    </div>
  <%end%>
</div>
